// =============================================================================
// 冒烟测试：验证 kych 包主模板完整渲染流程
// =============================================================================
// 运行要求：typst compile --root . --features html --format pdf test.typ
// =============================================================================
#import "/src/kych.typ" as kych

// 测试 1: 使用主模板渲染基本页面
#show: kych.kych-show.with(
  header-links: (
    ("/", "首页"),
    ("/blog/", "博客"),
    ("/docs/", "文档"),
  ),
  title: "测试页面",
  lang: "zh",
)

= 欢迎使用 跨越晨昏

这是测试页面内容。跨越晨昏 是一个基于 Tufte CSS 风格的静态网站生成器。

== 功能测试

以下测试验证模板的核心功能是否正常工作。

=== 数学公式

行内公式：$E = m c^2$ 嵌入在段落中。

块级公式：
$ sum_{i=1}^{n} i = (n(n+1)) / 2 $

=== 脚注与侧注

这段文字包含一个脚注#footnote[这是脚注内容，在 HTML 中会渲染为侧注。]。

#kych.margin-note[这是通过 margin-note 函数手动创建的侧注。]

=== 图表

#figure(
  table(
    columns: 2,
    [项目], [值],
    [速度], [光速],
    [质量], [静质量],
  ),
  caption: [物理常数表],
)

=== 全宽内容

#kych.full-width[
  这段内容使用 full-width 函数标记为全宽显示。
]

=== 交叉引用

在 @sec-ref 中可以看到更多细节。

== 参考章节 <sec-ref>

这里是参考章节的内容，用于测试交叉引用功能。如 @eq-test 所示。

$ E = m c^2 $ <eq-test>
